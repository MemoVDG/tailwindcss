module.exports = {
  theme: {
    extend: {
      // Agrega directivas
      fontFamily: {
        display: ['Gilroy', 'sans-serif'],
        body: ['Graphik', 'sans-serif']
      },
      // Se agregan cosas ademas de las que ya tiene por defecto
      extend: {
        colors: {
          cyan: "#9cbbff"
        }
      },
    },
    variants: {},
    plugins: [],
  }
}
